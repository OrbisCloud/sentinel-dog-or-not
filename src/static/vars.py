from pathlib import Path

path: Path = Path(Path('.').absolute()) / 'data' / 'neural_training_data.csv'
path2: Path = Path(Path('.').absolute()) / 'data' / 'ex2_data.csv'
pickle_file: Path = Path(Path('.').absolute()) / 'data' / 'network.pickle'